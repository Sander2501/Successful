# Pinned dependencies for the Successful trading bot.
#
# The core package (models, indicators, strategies, risk, backtester, metrics)
# is pure-stdlib and needs none of these; they are required only for live
# connectivity, config loading, and running the tests. Mirrors the dependency
# declarations in pyproject.toml, pinned for reproducible installs and
# dependency scanning. The heavier research extras (pandas/numpy/pyarrow/
# matplotlib) live under pyproject's [research] optional group, not here.

# --- runtime (live trading + config) ---
requests==2.33.1
websocket-client==1.9.0
PyYAML==6.0.1
python-dotenv==1.2.2

# requests transitive closure (pinned for complete dependency scanning)
certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
urllib3==2.6.3

# --- test ---
pytest==9.1.1
iniconfig==2.3.0
packaging==24.0
pluggy==1.6.0
